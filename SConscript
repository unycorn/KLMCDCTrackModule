Import('env')

env['LIBS'] = ['klm_dataobjects', 
               'framework', 
               'rawdata_dataobjects',
               '$ROOT_LIBS', 'cdc','cdc_dataobjects', '$ROOT_LIBS','mdst_dataobjects','tracking_dataobjects']

Return('env')
